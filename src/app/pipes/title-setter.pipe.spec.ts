import { TitleSetterPipe } from './title-setter.pipe';

describe('TitleSetterPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleSetterPipe();
    expect(pipe).toBeTruthy();
  });
});
